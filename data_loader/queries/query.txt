select time from scope_inputs where voltage < (
select avg(voltage) from scope_inputs where voltage >= 0.0) and
sea_data_test-# voltage > 0.0;

select time from scope_inputs where voltage > (
select avg(voltage) from scope_inputs where voltage < 0.0) and
voltage < 0.0;